__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/

# Local overrides only. The shipped copies live in evie/defaults/ and are the
# single source of truth -- anything at the repo root is your own working
# version, with your machine's paths and your enabled brains in it.
/brains.yaml
/config.yaml

# Personal by definition.
.evie/
vault/

# Model weights, downloaded by `evie doctor --fix`.
*.onnx
voices-*.bin
